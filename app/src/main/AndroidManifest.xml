<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="stanwang.top.select">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity android:name=".splash.SplashActivity" />
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".student.StudentHomeActivity" />
        <activity android:name=".student.SelectActivity" />
        <activity android:name=".student.RecordActivity" />
        <activity android:name=".admin.AdminActivity" />
        <activity android:name=".admin.StudentManageActivity" />
        <activity android:name=".admin.CourseManageActivity" />
        <!--<activity android:name=".admin.RecordManageActivity" />-->
        <activity android:name=".admin.SearchActivity" />
        <activity android:name=".admin.AvgClassActivity" />
        <activity android:name=".admin.AvgCourseActivity"></activity>
    </application>

</manifest>